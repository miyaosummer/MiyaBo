= flash[:succes]
= form_with(model: [@user,@asset],local: true) do |f|
  = f.label :name
  = f.select :name , @junres.map { |j| [j.name, j.name, {"ジャンル": @asset.name}] }
  ¥
  = f.text_field :money,placeholder:"---------------円",class:"assets__name"
  = f.submit
  <br>
  ＜設定済みの予算＞
  -@assets.each do |asset|
    <br>
    ="#{asset.name}:#{asset.money}円"
  